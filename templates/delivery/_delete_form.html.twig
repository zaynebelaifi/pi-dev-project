<form method="post" action="{{ path('app_delivery_delete', {'id': delivery.delivery_id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ delivery.delivery_id) }}">
    <button class="btn">Delete</button>
</form>
