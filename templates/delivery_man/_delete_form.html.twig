<form method="post" action="{{ path('app_delivery_man_delete', {'delivery_man_id': delivery_man.delivery_man_id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ delivery_man.delivery_man_id) }}">
    <button class="btn">Delete</button>
</form>
